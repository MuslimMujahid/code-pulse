## Codebase Patterns
- Next.js: useSearchParams() requires a Suspense boundary — wrap any client component using it in <Suspense> in the parent Server Component
- Next.js: Client components with Zustand store must be "use client" at top of file; imported store actions work in event handlers without extra wrappers
- Routing: loadRepo() success triggers redirect to /dashboard?repo=<path>; DashboardShell provides the full layout shell from US-010 onward
- Dashboard layout: HEADER_H=40px fixed top, SCRUBBER_H=56px fixed bottom, SIDEBAR_W=320px collapsible right panel; content area uses absolute positioning between the two fixed bars
- Sidebar toggle: uses CSS transform translateX(SIDEBAR_W) / translateX(0) with 200ms ease-out transition; main canvas uses margin-right matching sidebar width
- commitsCapped banner: 28px fixed bar between header and content area; uses top: HEADER_H + (commitsCapped ? 28 : 0) for content area positioning
- Package manager: pnpm (use pnpm for all installs/runs)
- Test runner: Vitest with jsdom environment; passWithNoTests: true to exit 0 with no tests
- TypeScript: strict mode, moduleResolution: bundler, paths alias @/* → ./src/*
- CSS: Tailwind v4 via @import "tailwindcss" in globals.css (no tailwind.config.js needed)
- Next.js: App Router, 15.x, src/app directory structure
- ESLint: flat config (eslint.config.mjs), requires @eslint/eslintrc package (not bundled with next)
- Quality checks: pnpm typecheck (tsc --noEmit), pnpm test (vitest run), pnpm lint (next lint)
- Env vars: GEMINI_API_KEY, GEMINI_MODEL, CODEPULSE_MAX_COMMITS documented in .env.example
- .gitignore must include .next/, tsconfig.tsbuildinfo, .ralph-gh/agent.lock, .ralph-gh/agent.log
- simple-git: simpleGit(path) throws synchronously when the directory doesn't exist — wrap instantiation in try/catch, not just checkIsRepo()
- simple-git: use git.raw(["log", "--format=MARKER...", "--numstat"]) for reliable per-commit file stat parsing; split output on the MARKER prefix
- Shared types live in src/lib/types.ts (CommitEntry, RepoData, ParseRepoResult, GraphNode, GraphEdge, GraphData etc.)
- GraphBuilder: buildGraph() is a pure function; coChangeKey helper is duplicated locally (not re-imported from git-parser) to avoid cross-module coupling
- GraphBuilder: lastCoChangeDate is computed by scanning commits oldest-to-newest and overwriting the map entry, so the last write is the most recent date
- GraphBuilder: fileToCommits entries are ordered oldest-first (matches git-parser's reversed output), so hashes[0] gives firstCommitDate directly
- CommitSummarizer: AI_UNAVAILABLE is a string sentinel const; server-side only; uses in-memory Map cache keyed by filePath::lastCommitHash; CHUNK_SIZE=50 commits per prompt chunk; consolidation pass when >1 chunk

---

## 2026-04-05 - US-001: Scaffold Next.js project with TypeScript, Tailwind, and test tooling
- What was implemented: Verified existing scaffold, installed dependencies via pnpm, fixed vitest config (passWithNoTests: true), installed missing @eslint/eslintrc, updated .gitignore to exclude .next/ and build artifacts
- Files changed: vitest.config.ts, .gitignore, package.json (added @eslint/eslintrc devDep), pnpm-lock.yaml
- **Learnings for future iterations:**
  - @eslint/eslintrc is NOT automatically installed with eslint-config-next — must be added explicitly as a devDependency
  - Vitest exits code 1 with "No test files found" by default; set passWithNoTests: true in vitest.config.ts
  - Node modules were empty at start — pnpm install needed before any quality checks
  - All 3 quality checks now pass: typecheck (tsc --noEmit), test (vitest run), lint (next lint)
---

## 2026-04-05 - US-002: Implement GitParser module with tests
- What was implemented: Created src/lib/types.ts with shared TypeScript types (CommitEntry, RepoData, ParseRepoSuccess, ParseRepoError, ParseRepoResult). Created src/lib/git-parser.ts with parseRepo() function and helper utilities. Created src/lib/git-parser.test.ts with 14 tests using programmatically created fixture git repos.
- Files changed: src/lib/types.ts (new), src/lib/git-parser.ts (new), src/lib/git-parser.test.ts (new)
- **Learnings for future iterations:**
  - simpleGit(path) throws SYNCHRONOUSLY when the directory doesn't exist — must wrap the instantiation call itself in try/catch, not just the async checkIsRepo() call
  - simple-git's log() doesn't reliably expose --numstat data in a structured way; use git.raw(["log", "--format=PREFIX%H|||%an...", "--numstat"]) and parse the raw string
  - git log outputs newest-first by default; reverse the parsed commits array for oldest-first ordering
  - co-change pair keys: sort([fileA, fileB]).join("|") — exported as coChangeKey() util for use in tests and downstream modules
  - Vitest fixture repos: use beforeAll/afterAll with mkdtemp + rm for temp git repos; configure user.email and user.name in the fixture git instance or commits fail
  - git --author flag in simple-git commit options: use { "--author": "Name <email>" }
---

## 2026-04-05 - US-003: Implement GraphBuilder module with tests
- What was implemented: Added GraphNode, GraphEdge, GraphData types to src/lib/types.ts. Created src/lib/graph-builder.ts with buildGraph() pure function. Created src/lib/graph-builder.test.ts with 18 tests using hand-crafted RepoData fixtures.
- Files changed: src/lib/types.ts (GraphNode, GraphEdge, GraphData types added), src/lib/graph-builder.ts (new), src/lib/graph-builder.test.ts (new)
- **Learnings for future iterations:**
  - buildGraph() builds a commitDateMap (hash→date) upfront for O(1) lookups; avoids rescanning commits per edge
  - lastCoChangeDate: scan commits oldest-to-newest writing to a Map per pair key; the last write is always the most recent date (no explicit date comparison needed)
  - fileToCommits is ordered oldest-first (git-parser reverses git log output), so hashes[0] is always the first commit
  - coChangeKey helper is duplicated locally in graph-builder.ts rather than importing from git-parser — keeps modules decoupled; both implementations are trivially identical
  - primaryContributor ties are broken by lexicographic author name ordering (deterministic behavior)
  - Tests use entirely in-memory RepoData fixtures — no file I/O, git repos, or async needed; runs in ~10ms
---

## 2026-04-05 - US-004: Implement TimelineFilter module with tests
- What was implemented: Created src/lib/timeline-filter.ts with filterByDate() pure function. Created src/lib/timeline-filter.test.ts with 24 tests covering node inclusion/exclusion, edge inclusion/exclusion, progressive timeline scenarios, purity guarantees, and edge cases.
- Files changed: src/lib/timeline-filter.ts (new), src/lib/timeline-filter.test.ts (new)
- **Learnings for future iterations:**
  - filterByDate() uses Date.parse() for ms-precision comparisons; avoids repeated string comparisons on every tick
  - O(n) for nodes (one pass), O(e) for edges (one pass after building an included-node Set) — no O(n²) scanning
  - Uses a Set<string> for O(1) node inclusion lookups when filtering edges
  - react-force-graph mutates edge source/target into node objects at runtime; the implementation guards against this by checking `typeof edge.source === "string"` and extracting `.id` if needed
  - Purity: function returns new arrays/objects; never mutates input graphData
  - Tests: use hand-crafted GraphData fixtures (makeNode/makeEdge helpers) — no async, no I/O; 24 tests run in ~11ms
---

## 2026-04-05 - US-005: Implement CommitSummarizer module with tests
- What was implemented: Created src/lib/commit-summarizer.ts with summarize() function and clearSummaryCache() export. Created src/lib/commit-summarizer.test.ts with 23 tests covering API unavailability, basic prompting, caching, chunking (CHUNK_SIZE=50), and error handling. All 79 tests pass.
- Files changed: src/lib/commit-summarizer.ts (new), src/lib/commit-summarizer.test.ts (new)
- **Learnings for future iterations:**
  - @google/generative-ai mock: use vi.mock() at the module level before importing the module under test; mock the class constructor with vi.fn().mockImplementation(() => ({ getGenerativeModel: vi.fn().mockReturnValue({ generateContent: mockFn }) }))
  - To test which model was used: inspect vi.mocked(GoogleGenerativeAI).mock.results[lastIdx].value.getGenerativeModel.mock.calls — the class is re-instantiated per summarize() call so always grab the last result
  - In-memory cache is keyed by filePath + lastCommitHash; clearSummaryCache() is exposed for test isolation
  - Chunking: commits.length > CHUNK_SIZE triggers multi-chunk path (2 chunk calls + 1 consolidation = 3 total generateContent calls for 55 commits with CHUNK_SIZE=50)
  - AI_UNAVAILABLE is a const string sentinel (not a class/symbol); type is `typeof AI_UNAVAILABLE`; allows API route to distinguish missing key from a real error without throwing
  - process.env mutations in tests need beforeEach reset and afterEach restore (spread ORIGINAL_ENV); delete process.env.GEMINI_API_KEY to test the undefined case
---

## 2026-04-05 - US-006: Implement POST /api/analyze route
- What was implemented: Created src/app/api/analyze/route.ts — a Next.js App Router POST handler that accepts { repoPath, commitLimit } in the request body, calls parseRepo() + buildGraph(), and returns { graphData, commitsCapped }.
- Files changed: src/app/api/analyze/route.ts (new)
- **Learnings for future iterations:**
  - Next.js App Router route handlers live in src/app/api/<path>/route.ts and export named async functions (POST, GET, etc.)
  - Import server-side modules (@/lib/git-parser, @/lib/graph-builder) directly — they are never shipped to the client
  - commitsCapped is computed by comparing repoData.commits.length === effectiveLimit (not > effectiveLimit, since parseRepo caps at exactly that number)
  - CODEPULSE_MAX_COMMITS env var is the fallback if no commitLimit is provided in the body; final fallback is 500
  - All quality checks pass with no changes needed to existing code
---

## 2026-04-05 - US-007: Implement POST /api/summarize route
- What was implemented: Created src/app/api/summarize/route.ts — a Next.js App Router POST handler that accepts { filePath, commits }, calls CommitSummarizer.summarize(), and streams the result back as plain text. Returns HTTP 503 { error: 'AI_UNAVAILABLE' } when GEMINI_API_KEY is missing. Returns HTTP 400 for missing/invalid parameters. Returns HTTP 500 for unexpected Gemini errors.
- Files changed: src/app/api/summarize/route.ts (new)
- **Learnings for future iterations:**
  - Streaming a pre-resolved string via ReadableStream: create a ReadableStream with a start() controller that enqueues the encoded result and immediately closes. This satisfies the "no buffering" requirement even when CommitSummarizer internally awaits the full Gemini response before returning.
  - Return `new Response(stream, { headers: { "Content-Type": "text/plain; charset=utf-8" } })` — NOT NextResponse.json() — for streaming text bodies.
  - AI_UNAVAILABLE check: compare result === AI_UNAVAILABLE (string sentinel), then return 503; all other errors from summarize() are caught and returned as 500.
  - The route is thin by design — all business logic (caching, chunking, API key handling) stays in CommitSummarizer; the route only handles HTTP concerns.
---

## 2026-04-05 - US-008: Implement Zustand AppStore with full state and actions
- What was implemented: Created src/store/app-store.ts with useAppStore Zustand store containing all application state (repoPath, graphData, filteredData, scrubberDate, selectedFile, viewMode, activeContributor, searchQuery, isLoading, error, commitsCapped) and all actions (loadRepo, setScrubberDate, setSelectedFile, setViewMode, setActiveContributor, setSearchQuery).
- Files changed: src/store/app-store.ts (new)
- **Learnings for future iterations:**
  - Store file must be marked "use client" since Zustand 5.x with create() in Next.js App Router requires client context
  - loadRepo sets scrubberDate to a far-future ISO date after loading so that all nodes/edges are visible by default (avoids needing to find the actual max date in graphData)
  - setViewMode switches viewMode AND clears activeContributor when switching away from 'contributor' mode — both in a single set() call
  - filterByDate is imported from @/lib/timeline-filter and called synchronously in setScrubberDate — no async needed; this is the client-side performance optimization
  - AppState interface exported separately from useAppStore for use in components that need type-only imports
  - ViewMode type exported as a named type for use in component props
---

## 2026-04-05 - US-009: Build RepoLoader landing page with path input and URL persistence
- What was implemented: Created src/components/RepoLoader.tsx — a "use client" form component with: repo path text input, Analyze button, loading state (spinner + disabled input), error display, ?repo= URL persistence via useRouter/useSearchParams, pre-fill from URL on mount, and redirect to /dashboard?repo=<path> once graphData loads. Created src/components/DashboardShell.tsx as a minimal placeholder for US-010. Updated src/app/page.tsx to render RepoLoader inside a Suspense boundary (required by useSearchParams). Created src/app/dashboard/page.tsx to host the future dashboard.
- Files changed: src/components/RepoLoader.tsx (new), src/components/DashboardShell.tsx (new), src/app/page.tsx (updated), src/app/dashboard/page.tsx (new)
- **Learnings for future iterations:**
  - useSearchParams() in Next.js App Router REQUIRES a parent <Suspense> boundary — without it, the build warns and the component won't SSR; always wrap in page.tsx Suspense
  - router.replace() with { scroll: false } is the right call for updating the URL without adding to browser history when user is still on the same page
  - Zustand store actions (loadRepo, isLoading, error, graphData) are accessed directly from useAppStore() in the client component — no prop drilling needed
  - useTransition + startTransition wraps the router.push() to avoid blocking the UI while Next.js prepares the dashboard route
  - Button disabled state via HTML disabled attr (not just opacity CSS) ensures keyboard/screen-reader users also can't submit an empty form
  - Deep space dark theme: background #0d0d1a applied via inline style (not Tailwind) to ensure consistency with globals.css :root variable; dot-grid backdrop via radial-gradient backgroundImage with pointer-events-none + fixed positioning
---

## 2026-04-05 - US-010: Build dashboard layout shell with graph area, scrubber, sidebar slot, and header
- What was implemented: Replaced the placeholder DashboardShell stub with the full dashboard layout. Implemented all 4 fixed layout regions: header (40px top), commitsCapped warning banner (28px, conditional, below header), graph canvas area (main, fills remaining space), fixed scrubber bottom bar (56px). Added collapsible right sidebar panel (320px) with CSS transform-based slide animation. Added top-right ViewControls slot with sidebar toggle button and placeholder. Header shows CodePulse brand, shortened repo path (last 2 segments), node/edge stats, and a "Change repo" button that routes back to /?repo=<path>. commitsCapped banner shows amber warning with commit limit. Graph canvas has a subtle grid overlay as placeholder.
- Files changed: src/components/DashboardShell.tsx (full implementation replacing stub)
- **Learnings for future iterations:**
  - Dashboard uses height:100dvh + overflow:hidden on root div; fixed header+footer bars with absolute-positioned content area between them
  - Sidebar slides via `transform: translateX(SIDEBAR_W)` → `translateX(0)` with 200ms ease-out; main canvas uses `marginRight: sidebarOpen ? SIDEBAR_W : 0` to adjust its width so the graph canvas doesn't get hidden behind the sidebar
  - commitsCapped banner: 28px high, slotted between header and content area; content top offset is `HEADER_H + (commitsCapped ? 28 : 0)` with a CSS transition for smooth banner show/hide
  - Repo path shortening: split on "/" and take last 2 segments — avoid very long paths in the header without losing context
  - US-015 (ViewControls) slots into #view-controls-slot div in top-right corner of the graph canvas; US-018 (TimelineScrubber) replaces the placeholder TIMELINE bar in the fixed bottom bar
  - Browser-tested: header, main, aside, scrubber bar all confirmed; sidebar translateX toggles correctly; Change repo routes to /?repo=... with encoded path; all 79 tests pass
---

## 2026-04-05 - US-011: Build ForceGraphCanvas with basic node and edge rendering
- What was implemented: Rewrote ForceGraphCanvas.tsx to use the `force-graph` library directly via an imperative useEffect pattern (dynamic import, new ForceGraph(element), resize observer, _destructor() cleanup). Integrated ForceGraphCanvas into DashboardShell with handleNodeClick and handleBackgroundClick callbacks. Added `force-graph` as a direct dependency in package.json and created a symlink in node_modules/.
- Files changed: src/components/ForceGraphCanvas.tsx (full rewrite), src/components/DashboardShell.tsx (ForceGraphCanvas integration), package.json (force-graph direct dep), node_modules/force-graph (symlink)
- **Learnings for future iterations:**
  - CRITICAL: `react-force-graph` causes `AFRAME is not defined` crash — its .mjs entry imports `3d-force-graph-vr` → `aframe-forcegraph-component`, which calls `AFRAME.registerComponent(...)` at module-init time. AFRAME is not globally defined in Next.js, so this throws a client-side exception that React error boundary catches, unmounting the entire component tree. Do NOT use react-force-graph.
  - Use `force-graph` (the underlying 2D-only library) directly with dynamic import inside useEffect: `const { default: ForceGraph } = await import("force-graph")`. This avoids SSR issues and the AFRAME dependency entirely.
  - force-graph imperative pattern: instantiate with `new ForceGraph(containerRef.current)`, configure via chained .graphData().nodeColor()... methods, clean up with `fg._destructor()` in the useEffect return function.
  - force-graph exposes `fg.width(w).height(h)` for resize; use a ResizeObserver on the container div to update on layout changes.
  - Logarithmic node sizing: `Math.max(2, Math.log1p(node.commitCount) * 2.5)` — prevents tiny 1-commit nodes and avoids oversized outliers.
  - Blue-violet gradient by commitCount rank: sort nodes by commitCount, assign t ∈ [0,1], interpolate #3b82f6 → #8b5cf6 using hex channel lerp.
  - Future nodes (in graphData but not filteredData): pre-compute a Set<string> of filtered node IDs, render absent nodes at 20% opacity in gray (#374151).
  - HMR stale chunk issue in Playwright dev testing: in dev mode, Playwright's browser has stale HMR manifest hashes from previous server sessions; 404s trigger Fast Refresh full reload, resetting Zustand state. Workaround: always browser-verify in production mode (pnpm build && pnpm start).
  - SPA navigation: Next.js App Router router.push() is client-side — no full page load event. Use page.wait_for_url("**/dashboard**", wait_until="commit") not "load" in Playwright.
  - ?repo= URL param approach: navigate to /?repo=<path> in Playwright; RepoLoader's useEffect reads the param and fills the input without needing page.fill() (which doesn't trigger React onChange).
  - Browser verification (production mode): canvas renders ✓, 71 nodes · 954 edges in header stats ✓, no JS errors ✓, sidebar aria-hidden=true ✓ — PASS
---

## 2026-04-05 - US-012: Add node labels, hover tooltips, and amber edge pulsing to ForceGraphCanvas
- What was implemented: Extended ForceGraphCanvas.tsx with: (1) node labels rendered via nodeCanvasObject with screen-diameter threshold (> 8px), fade-in gradient, monospace font, semi-transparent pill background; (2) HTML tooltip via nodeLabel prop showing full file path, commit count, and primary contributor name; (3) edge coloring via linkColor callback — amber rgba(245,158,11,0.7) for edges within 90 days of scrubberDate, muted slate rgba(55,65,81,0.5) for older edges; (4) scrubberDate passed as new prop from DashboardShell, stored in a ref so linkColor callback always has the latest value without graph re-init.
- Files changed: src/components/ForceGraphCanvas.tsx (labels, tooltip, edge coloring, scrubberDate prop), src/components/DashboardShell.tsx (pass scrubberDate prop)
- **Learnings for future iterations:**
  - force-graph's `nodeLabel` prop accepts an HTML string or HTMLElement — use it to render rich tooltip HTML (styled divs) that the library injects into a floating tooltip container. The tooltip is positioned automatically by force-graph near the cursor.
  - Node label zoom threshold: use `screenDiameter = 2 * nodeRadius(commitCount) * Math.sqrt(globalScale)`. When `screenDiameter > 8`, render the label. Add a smooth fade-in with `globalAlpha = Math.min(1, (screenDiameter - 8) / 8)`.
  - Font sizing for zoom: `fontSize = Math.max(8, Math.min(12, 10 / Math.sqrt(globalScale)))` keeps labels readable across zoom levels.
  - Label pill background: `ctx.fillRect(x - textWidth/2 - pad, textY - pad, textWidth + pad*2, fontSize + pad*2)` must be drawn BEFORE the text fillText call.
  - Edge recency: `isRecentEdge(lastCoChangeDate, cutoffDate)` → `cutoffMs - edgeMs <= 90 * 24 * 60 * 60 * 1000` — simple ms arithmetic, no date library needed.
  - scrubberDate in linkColor callback: store in a `useRef` updated by a separate `useEffect`. Since linkColor is configured once at mount time (not re-bound on prop change), the ref pattern gives the callback access to the latest value without requiring graph re-initialisation.
  - CRITICAL stale server bug: if a `pnpm start` server from a previous session is still running (even silently), it serves RSC payloads with old chunk hashes baked in. The new `pnpm build` generates correct static files on disk, but the OLD server process never reloads — it continues serving the old RSC manifest mapping `DashboardShell` to the old chunk hash. Always kill all `next-server`/`pnpm start` processes before re-testing. Detection: `curl -H "RSC: 1" http://localhost:3000/dashboard` and check the chunk hash in the response.
  - Browser verification: canvas renders ✓, 72 nodes · 957 edges ✓, node labels visible at zoom (layout.tsx, pnpm-lock.yaml, globals.css, tsconfig.json) ✓, amber edges on recent co-changes ✓, muted gray on dormant edges ✓, no JS errors ✓ — PASS
