"use client";

/**
 * DashboardShell — placeholder layout for the dashboard.
 *
 * This is a minimal stub so that the RepoLoader page can redirect here
 * after analysis completes. The full implementation is in US-010.
 */
export function DashboardShell() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: "#0d0d1a", color: "#e2e8f0" }}
    >
      <p className="text-sm" style={{ color: "#475569" }}>
        Dashboard — coming soon (US-010)
      </p>
    </div>
  );
}
